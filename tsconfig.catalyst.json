{
  "//": "Typecheck for this workspace's own code. The root tsconfig.json includes only shared/** and is frozen to this workspace by order 0002, so this extends it rather than editing it. Run: npx tsc --noEmit -p tsconfig.catalyst.json",
  "extends": "./tsconfig.json",
  "include": ["catalyst/**/*.ts", "functions/**/*.ts", "shared/**/*.ts"],
  "//exclude": "Vendored copies are byte-identical to functions/_lib, which is already checked here. Typechecking both would duplicate every declaration and report errors twice.",
  "exclude": ["functions/*/_vendor/**"]
}
